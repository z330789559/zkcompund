//!
//! The 'swap' contract entry.
//!

contract Swap {
    pub value: u64;

    pub fn new(value: u64) -> Self {
        Self {
            value: value,
        }
    }

    pub fn getV(self) -> u64 {
        self.value
    }
}
