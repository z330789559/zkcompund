//!
//! The 'tst' contract entry.
//!

contract Tst {
    pub value: u64;

    pub fn new(value: u64) -> Self {
        Self {
            value: value,
        }
    }

    pub fn get(self) -> u64 {
        20 as u64
    }
}
